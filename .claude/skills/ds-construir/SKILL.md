---
name: ds-construir
description: Cria ou evolui componentes no Figma seguindo a arquitetura correta de tokens do Olist Design System (base → theme → component → componente). Faz auditoria dos tokens necessários, propõe o plano para aprovação, constrói o component set com Auto Layout e bind de variáveis, e organiza a grade de variantes. Termina no Figma — não gera código React.
version: 1.0
---

# ds-construir

Executa o **Caso 5** da skill `olist-ds-specialist`.

## O que fazer

O usuário forneceu uma intenção textual (novo componente) ou uma URL do Figma (evolução de componente existente). Construa ou evolua o componente no Figma com a arquitetura correta de tokens.

## Passos obrigatórios

1. Carregar a skill principal: ler `.claude/skills/olist-ds-specialist/SKILL.md`
2. Executar o **Caso 5** dessa skill na íntegra
