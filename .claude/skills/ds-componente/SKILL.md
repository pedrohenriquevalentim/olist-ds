---
name: ds-componente
description: Implementa um novo componente para o Olist Design System a partir de uma URL do Figma. Gera os 6 arquivos obrigatórios (tsx, module.css, test.tsx, stories.tsx, metadata.json, index.ts) — o metadata.json passa por um gate de aprovação do usuário antes do código — e cria o frame de documentação no Figma em paralelo.
version: 1.0
---

# ds-componente

Executa o **Caso 7** da skill `olist-ds-specialist`.

## O que fazer

O usuário forneceu uma URL do Figma de um componente do DS. Implemente-o como componente React completo para o repositório `olist-ds`.

## Passos obrigatórios

1. Carregar a skill principal: ler `.claude/skills/olist-ds-specialist/SKILL.md`
2. Executar o **Caso 7** dessa skill na íntegra
