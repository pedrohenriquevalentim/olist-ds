import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Foundations/Typography" />

# Tipografia

A fonte primária é **Plus Jakarta Sans**, carregada via Google Fonts.

## Font Family

| Token | Valor |
|-------|-------|
| `--font-family-jakarta` | Plus Jakarta Sans |
| `--font-family-arial` | Arial (fallback) |

## Font Sizes

<div style={{ display: 'flex', flexDirection: 'column', gap: 16 }}>
  <span style={{ fontSize: 10 }}>10px — Caption (--font-size-10px)</span>
  <span style={{ fontSize: 12 }}>12px — Small (--font-size-12px)</span>
  <span style={{ fontSize: 14 }}>14px — Body Small (--font-size-14px)</span>
  <span style={{ fontSize: 16 }}>16px — Body (--font-size-16px)</span>
  <span style={{ fontSize: 20 }}>20px — Heading 5 (--font-size-20px)</span>
  <span style={{ fontSize: 24 }}>24px — Heading 4 (--font-size-24px)</span>
  <span style={{ fontSize: 32 }}>32px — Heading 3 (--font-size-32px)</span>
  <span style={{ fontSize: 40 }}>40px — Heading 2 (--font-size-40px)</span>
  <span style={{ fontSize: 48 }}>48px — Heading 1 (--font-size-48px)</span>
</div>

## Font Weights

<div style={{ display: 'flex', flexDirection: 'column', gap: 12, fontSize: 20 }}>
  <span style={{ fontWeight: 200 }}>Extra Light (200)</span>
  <span style={{ fontWeight: 300 }}>Light (300)</span>
  <span style={{ fontWeight: 400 }}>Regular (400)</span>
  <span style={{ fontWeight: 500 }}>Medium (500)</span>
  <span style={{ fontWeight: 600 }}>Semibold (600)</span>
  <span style={{ fontWeight: 700 }}>Bold (700)</span>
  <span style={{ fontWeight: 800 }}>Extra Bold (800)</span>
</div>